language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t myapp -f ./front/Dockerfile.dev ./front

script:
  - docker run -e CI=true myapp npm run test

after_success:
  - docker build -t docker-front ./front
  - docker build -t docker-back ./back
  - docker build -t docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "DOCKER_HUB_ID" --pasword-stdin

  - docker push docker-front
  - docker push docker-back
  - docker push docker-nginx